[build]
  command = "npx prisma generate && npx prisma migrate deploy && npx tsx prisma/seed.ts && npx tsx prisma/import-products.ts && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
