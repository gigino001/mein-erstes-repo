[build]
  base = "website"
  command = "npm run build && mkdir -p .next/server/chunks && cp src/generated/prisma/libquery_engine-*.so.node .next/server/chunks/"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
